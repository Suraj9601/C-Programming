#include<stdio.h>
int main() 
{
    int a,b,product;
    printf("Enter the number a :");
    scanf("%d",&a);

    printf("Enter the number b :");
    scanf("%d",&b);

    product = a * b;

    printf
    ("The product of %d and %d is %d",a,b,product);

    return 0;
}